import { SkillTreePipe } from './skill-tree.pipe';

xdescribe('SkillTreePipe', () => {
  it('create an instance', () => {
    const pipe = new SkillTreePipe();
    expect(pipe).toBeTruthy();
  });
});
